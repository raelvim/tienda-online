[build]
  publish = "."

# Redirigir /api/* al backend de Render
[[redirects]]
  from = "/api/*"
  to = "https://tienda-online-josy.onrender.com/api/:splat"
  status = 200
  force = true

# Redirect SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers de seguridad
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
